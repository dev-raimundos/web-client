services:
  api:
    build:
      context: ../../
      dockerfile: docker/dev/Dockerfile
    container_name: neon-vertex-api
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - api_logs:/app/logs

volumes:
  api_logs:
