services:
  web:
    build:
      context: ../../
      dockerfile: docker/dev/Dockerfile
    container_name: neon-vertex-web
    restart: unless-stopped
    ports:
      - "4200:4200"
    volumes:
      - ../../src:/app/src
      - ../../public:/app/public
